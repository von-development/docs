<CodeGroup>
    ```bash npm
    npm install {packages}
    ```

    ```bash pnpm
    pnpm install {packages}
    ```

    ```bash yarn
    yarn add {packages}
    ```

    ```bash bun
    bun add {packages}
    ```
</CodeGroup>
